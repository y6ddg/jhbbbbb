apiVersion: v1
kind: Service
metadata:
  name: jhbbbbb
  annotations:
    dev.okteto.com/auto-ingress: "true"
spec:
  type: ClusterIP  
  ports:
  - name: "jhbbbbb"
    port: 8080
  selector:
    app: jhbbbbb
